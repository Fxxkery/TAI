# Temporal Repair Agency — Phase 5.5 (TVA Theme, Tauri v2)

**What’s in this build**
- Title added to top bar (TVA style)
- Era Resources moved to toggle row (click-to-open, hidden until era can generate)
- Era cards: Agents + `+X/sec`, centered "Buy {Agent}" label, cost on button
- Middle panel shows TVA timeline background when idle
- Stats overlay in menu (totals & counts)
- Dev/testing: generation running; no TypeScript
- Build: `npm install`, `npm run tauri:dev`, `npm run tauri:build`